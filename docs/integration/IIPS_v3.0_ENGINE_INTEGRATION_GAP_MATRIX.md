# IIPS v3.0 Engine Integration — Integration Gap Matrix (E2E-025 → E2E-029)

**Date:** 2026-09-04  
**Branch:** `arena/01a06c00-iips-review-recovered` (HEAD `c65d533`)  
**Read-only phase output — no code changes yet**  
**Authority baseline:** Program v1.1 LTS (`program-v1.1.0` — 10 certified engines IES-006…015), not the hypothetical IES-020/016/017 described in the prompt.

> Classification key (per prompt §5): `IMPLEMENTATION` · `TEST` · `EVIDENCE` · `INTEGRATION` · `AUTHORITY BLOCK` · `ENVIRONMENT BLOCK`  
> Prompt binding: E2E-013 remains the control plane; E2E-025–029 are parallel implementation tracks; E2E-030 (Engine E2E Certification) is a later gate and is NOT claimed here.

---

## Matrix

| Workstream | Current evidence (repo) | Gap | Existing implementation | Required change (narrow, auditable) | Authority / Environment dependency |
|------------|------------------------|-----|------------------------|--------------------------------------|-----------------------------------|
| **E2E-025 API — Completed-engine API Integration** | API chain **partially complete**: `PlatformApi` + `RuntimeCoordinator` + `Transport` + `EvidencePipeline` + `SnapshotService/Store` verified (13 unit + 65 regression). `frontend/server/executive-transport.ts` runs all 10 certified engines over `PROGRAM_v1.1_REPLAY_BASELINE.json` and serves `GET /api/executive|portfolio|cross-sector|decision-matrix|company/:id|evidence/:id|replay/:id` (all proven provenance `FRESHNESS=SNAPSHOT`, `calibratedAt`, `transportSemantics: 1:1`). | **API completeness** — no **public** engine-registry / direct-dispatch surface: (a) no `GET /api/engines` listing `engineId ↔ IES ↔ sectorFamily ↔ capabilities` for non-admin consumers; (b) no `POST /api/engines/:engineId/execute` that validates request → `RuntimeCoordinator` dispatch → execution → snapshot/evidence → DTO+provenance → audit (current flows are *indirect* via the computed baselines). Also: `PlatformApi` `ApiRequest` (`tenantId/principal/inputs`) is distinct from the executive transport's baseline-only inputs. | `iips-platform/src/distributed/PlatformApi.ts` (version gate `1.0`, `security.authorize`, rate-limit 100, marketplace `uncertified-capability` gate); `src/runtime/RuntimeCoordinator.ts`; `src/framework/transport/Transport.ts` (`validate`); `frontend/server/executive-transport.ts` (`ENGINE_FACTORY`, `computeCertifiedPlatform`) | **IMPLEMENTATION** — Add additive, versioned engine integration adapter: `iips-platform/src/integration/EngineRegistry.ts` (certified IES↔engineId map, frozen), `EngineApiAdapter.ts` (`listEngines` + `execute(engineId, inputs)` via `RuntimeCoordinator` → `ExecutionResult` + snapshot/evidence refs + provenance, with validation + `Transport.validate` + `EvidencePipeline.validate`), and wire **two additive** HTTP routes in the transport (`GET /api/engines`, `POST /api/engines/:engineId/execute`). Keep all existing routes unchanged; use fixed-clock/deterministic-id deterministic path; no scoring/methodology change. | None — builds on existing frozen contracts. If a future requirement demands changing a freeze manifest or creating an IES-020/016/017 engine, stop → **AUTHORITY BLOCK** (see bottom row). Environment: `G:\IIPS` is **ENVIRONMENT BLOCK** if invoked — not used; repo JSON is the oracle. |
| **E2E-026 UI — Completed-engine UI Integration** | UI chain **complete** for all 10 sectors: `src/features/company/CompanyIntelligence`, `cross-sector/CrossSectorIntelligence`, `evidence/EvidenceExplorer`, `replay/ReplayExplorer`, `executive/ExecutiveDashboard`, `portfolio/PortfolioWorkspace`, plus `src/api/*.ts` typed clients (semantically inert) and `StateComponents` (Loading/Error/Unavailable) + `CertifiedBadge/FreshnessBadge`. Tests cover `loading→success→error`, `pillars-unavailable` (never fabricated), and decision distribution. | **UI hardening** — no user-facing engine-registry view and no direct `engine → API → UI` *dispatch* control (today Company/CrossSector etc. fetch the computed cross-sector DTOs). The UI does not yet expose the new `GET /api/engines` registry data or demonstrate `UI → POST /api/engines/:id/execute → render` for a chosen certified engine. | `frontend/src/features/**`, `frontend/src/api/**`, `frontend/server/executive-transport.ts` proxy | **TEST + EVIDENCE + narrow UI IMPLEMENTATION** — (1) add `frontend/src/api/engines.ts` typed client for `GET /api/engines`; (2) add a minimal, additive UI affordance (registry chip or route segment) that lists engines with `engineId/sectorFamily/IES/engineVersion` and provenance, reusing existing `Badges/StateComponents` (no product-shell redesign); (3) add UI integration tests verifying `UI → API → Engine → API response → UI rendering` plus `loading/error/provenance/identity/version` states. | None — presentation-only, uses existing contracts. Authority block if asked to redefine product shell or invent new capability taxonomy. |
| **E2E-027 Evidence / Provenance Integration** | **Complete per platform contracts:** `EvidencePackage { evidenceId, engineId, recommendation, compositeScore, confidence, keyMetrics, supportingScores, calibrationVersion, decisionRulesApplied, replayReference, provenance{frameworkVersion, engineVersion, methodologyVersion, snapshotId}, generatedAt }` + `Snapshot { snapshotId, engineId, schemaVersion, generatedAt, metrics, scores, verdict, evidenceRefs, provenance }` + `TransportDTO { metadata{transportVersion, engineId, schemaVersion, generatedAt, checksum}, rows }`. HTTP DTOs already carry `provenance{dataSource, freshness, calibratedAt, transportSemantics}`. | **Evidence-chain surfacing** — no end-to-end **E2E-027** demonstration artifact capturing `Engine output → provenance metadata → API response → UI consumption → audit/evidence record` as a single exercised chain with durable evidence. The LTS notes two *known* per-engine variance gaps that will be visible in this chain: R2 `engineVersions` staleness and R3 calibration-version only for Technology — must NOT be silently patched; they must be evidenced as known deviations. | `src/framework/evidence/EvidencePipeline.ts`, `src/snapshot/SnapshotService.ts`, `frontend/server/executive-transport.ts` `computeCertifiedEvidence` (already builds `decision→evidence→snapshot→replay→provenance`), UI `EvidenceExplorer/ProvenanceChain` | **EVIDENCE + TEST** — Add `iips-platform/src/integration/EvidenceIntegration.test.ts`-style regression that for every certified engine asserts: (a) `Execute → Snapshot.engineId/IesMap correct`, (b) `Evidence.provenance.*` present + frozen, (c) `API provenance.freshness==='SNAPSHOT'`, (d) `UI ProvenanceChain` renders framework/engine/methodology/snapshot ids, (e) known R2/R3 variances are preserved and reported (not patched). No new provenance semantics; if existing schema proved insufficient, record **authority/design dependency** (prompt §8). | Recording deficiency → **AUTHORITY BLOCK** (do not silently change governance model). |
| **E2E-028 Replay / Provenance E2E** | **Replay certified:** `ReplayService.replay(snapshotId) → { reproduced:true, byteIdentical:true, evidenceRefs }`, `replayAll()`, `snapshot-replay.test.ts`; program-level `PROGRAM_v1.1_TRACK3_REPLAY_CERTIFICATION.md` byte-identical for all 10 sectors; UI `ReplayExplorer` shows `reproduced/byteIdentical` + `replay-equivalence MATCH` and the *governed* note *"No field-level diff is available"* (hard stop honored — no invented metric diff). | **Replay-E2E demonstration** — no single captured trace showing `original execution → captured evidence/provenance → replay input/context → replay execution → comparable governed result → recorded replay evidence` as executed via the **HTTP API** (dispatch → snapshot → replay) plus the same via `DistributedRuntime`/`PlatformApi`. Need deterministic + attributable replay verification packaged as evidence. | `src/replay/ReplayService.ts`, `src/runtime/RuntimeCoordinator.ts` (`replay()`), `src/distributed/DistributedRuntime.ts` (`materializeContext`, `provisionNode`, `execute`), `frontend/server/executive-transport.ts` `computeCertifiedReplay`, `frontend/src/features/replay/ReplayExplorer.tsx` | **TEST + EVIDENCE** — Add `iips-platform/tests/integration/replay-e2e.test.ts` that: (1) executes each certified engine via `RuntimeCoordinator` → stores snapshot → `ReplayService.replay(snapshotId)` and asserts `byteIdentical` + `evidenceRefs` match; (2) does the same through `PlatformApi.execute` and `DistributedRuntime` to prove no alternate-path divergence; (3) exercises `GET /api/replay/:sector` and verifies `original.verdict/composite` ↔ `replay.reproduced/byteIdentical` + `provenance` intact. **Do not claim certification** — record `implemented/verified/evidenced vs certified` separately. | None — replay semantics are frozen. Any need to amend replay contract → **AUTHORITY BLOCK**. |
| **E2E-029 CSIP Product / E2E Integration** | **CSIP integration certified:** `src/sector-engines/cross-sector/CrossSectorEngine.ts` (common pipeline ontology→intelligence→ranking→allocation→diversification→opportunity→correlation→evidence→reporting), `CSIP_FREEZE_MANIFEST.json`, `PROGRAM_v1.1_TRACK6_CSIP_CERTIFICATION.md` (sector-neutral: 10 ontology 8/8, zero CSIP branch). Executive transport already computes `CrossSectorEngine.run()` over derived `EngineOutput[]` and serves `GET /api/executive|portfolio|cross-sector` (CSIP DTOs) → `CrossSectorIntelligence` UI. | **Product-E2E evidence** — no dedicated `sector engine result → CSIP aggregation → API → product/UI → evidence/provenance → audit` **E2E slice test** that proves every certified engine participates via ontology registration and that CSIP outputs are consumed product-level (no duplicate sector engines). | `src/sector-engines/cross-sector/CrossSectorEngine.ts` + `OntologyMapper`, `frontend/server/executive-transport.ts` `computeCertifiedCrossSector/Executive/Portfolio`, `frontend/src/features/cross-sector`, `executive`, `portfolio` | **INTEGRATION + TEST** — Add `iips-platform/tests/integration/csip-product-e2e.test.ts` + `frontend/tests` extension that: (a) builds `EngineOutput[]` from the 10 real engine `composite/quality/risk/growth` (via golden pillars + ontology mapper), (b) runs `CrossSectorEngine.run(Balanced)` and asserts ranking/allocation/diversification/correlation/evidence are deterministic and ontology-driven, (c) hits `GET /api/cross-sector|executive|portfolio` and verifies CSIP DTO provenance (`dataSource` mentions certified v2.0 platform + frozen replay baseline), (d) verifies UI distribution/opportunity/ranking match the certified DTOs. Do not redefine CSIP methodology or create duplicate sector engines. | Taxonomy / methodology change → **AUTHORITY BLOCK**. |
| **E2E-030 Engine E2E Certification** | Final program cert issued: `PROGRAM_v1.1_FINAL_READINESS_CERTIFICATE.md` (9/9 Tracks, 10/10 engines, `program-v1.1.0` LTS). **No E2E-030 certification exists** for `Materials/Telecom/Auto` (they are not in this repo). | **Not implementable in this track** — certification convergence gate, later than E2E-025..029 | (none) | **EVIDENCE only** — capture `implemented/verified/evidenced vs still-uncertified` distinction for this report; **do not mark E2E-030 complete**. Report readiness + unresolved issues. | **AUTHORITY BLOCK** if asked to claim certification. |

---

## Cross-Cutting Gaps (affecting all E2E-025..029)

| Gap | Classification | Note |
|-----|----------------|------|
| Prompt hypothetical engines (Materials IES-020, Telecom IES-016, Auto IES-017) treated as certified | **AUTHORITY BLOCK** | Repository proves otherwise (planned, not certified). Track explicitly **will not** create those engines. Implementation uses the 10 real certified engines (IES-006…015) and services remain general — adding a real IES-016/017/020 later is **zero** CSIP/platform change (ontology registration only) but requires a new freeze manifest + authority. |
| `E2E-013 / A2→A1` control plane referenced in prompt | **EVIDENCE** | Not present as an artifact in this recovery checkout. Control plane realized as **Program v1.1 LTS Baseline** + per-sector `IES-*_FREEZE_MANIFEST.json`. Track stops on any conflict with those — does not resolve by implementation. |
| `G:\IIPS` Windows checkout | **ENVIRONMENT BLOCK** | Not available in Arena/Linux. All integration uses the mounted repo; any future dependency is surfaced, not reconstructed. |

---

## Implementation Order (prompt §16, after this matrix)

`Phase 2 E2E-025 API → Phase 3 E2E-026 UI → Phase 4 E2E-027 Evidence → Phase 5 E2E-028 Replay → Phase 6 E2E-029 CSIP → Phase 7 Integrated testing + evidence → Phase 8 Report E2E-030 readiness`

Changes are **narrowly scoped to Engine Integration** (no governance amending, no regression-weakening, no taxonomy/scoring changes, no freeze-manifest edits).
