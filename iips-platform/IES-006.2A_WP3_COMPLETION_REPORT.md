# IES-006.2A — Banking Engine (WP-3) Completion Report

**Milestone:** IES-006.2A (WP-3 — Banking Engine)
**Repository:** `iips-platform` (implementation) — separate from `iips-engineering-standards` (specification)
**Status:** COMPLETE — `tsc` clean, **35/35 tests pass**. Awaiting approval before WP-4 (Validation / Release).
**Date:** 2026-08-06
**Baseline:** IES-005 + IES-005.1 (contracts) + IES-006 v1.0 (frozen, tag `v1.0.0`) + `IMPLEMENTATION_API_BASELINE.md`
**Prior:** WP-1 (Runtime) Approved · WP-2 (Framework) Approved

---

## 1. Scope delivered (WP-3 — Banking Engine, using frozen assets only)

| Deliverable | Implemented |
|---|---|
| Metric evaluation | ✅ `metrics/BankingMetrics.ts` (BM-001…BM-006 + capital BM-014/BM-015) |
| Score calculation | ✅ `scoring/BankingScoreEngine.ts` (band→score→pillar→composite) |
| Calibration profile loading | ✅ `calibration/BankingCalibration.ts` (loads frozen `banking-calibration-1.0.0.json`) |
| Decision pipeline | ✅ `decision/BankingDecision.ts` (composite→verdict + overrides) |
| Evidence generation | ✅ `evidence/BankingEvidence.ts` |
| Engine plugin | ✅ `BankingEngine.ts` (implements `SectorPlugin`, consumes platform services) |
| Public barrel | ✅ `index.ts` |

## 2. Files created

```
src/sector-engines/banking/
├── index.ts
├── BankingEngine.ts
├── metrics/    BankingMetrics.ts
├── scoring/    BankingScoreEngine.ts
├── calibration/ BankingCalibration.ts
├── decision/   BankingDecision.ts
├── evidence/   BankingEvidence.ts
└── frozen-assets/  (frozen calibration + golden dataset + expected outputs — read-only)
tests/regression/
├── banking-helpers.ts
└── banking-acceptance.test.ts   (the 4 WP-3 acceptance criteria)
```

## 3. Implementation API baseline honored

`IMPLEMENTATION_API_BASELINE.md` was created (frozen list of consumable interfaces). The Banking Engine **implements `SectorPlugin` and consumes** `Container`/`RuntimeCoordinator`/`EvidencePipeline`/`PluginLoader` — it does **not** bypass or re-implement runtime/framework/transport/registry/snapshot/replay/diagnostics/qualification/activation. No frozen platform layer was modified (only additive: `Container.register`, `Dependencies.runtimeCoordinator`, tsconfig `resolveJsonModule`).

## 4. WP-3 acceptance criteria — all four demonstrated

| Criterion | Result |
|---|---|
| **1. Golden Dataset Reproducibility** — every frozen golden bank reproduces the frozen expected outputs | ✅ All 5 banks match exactly (BK-001 Watch 47.1, BK-002 Buy 72.2, BK-003 Hold 58.6, BK-004 Buy 74.8, BK-005 Avoid 33.8) |
| **2. Replay Determinism** — same dataset twice → identical evidence + verdicts | ✅ identical verdict, composite, evidenceRef across runs |
| **3. Calibration Isolation** — changing calibration profile changes behavior without code changes | ✅ same metrics, two profiles → different verdicts (Buy vs Hold) purely from calibration |
| **4. Evidence Completeness** — verdict traceable Metric→Band→Score→Pillar→Composite→Verdict | ✅ evidence package captures metrics, pillars, composite, verdict, calibration version, provenance; immutable + valid |

## 5. Definition of Done

| DoD item | Status |
|---|---|
| Acceptance criteria satisfied | ✅ (4/4) |
| Unit tests added | ✅ |
| Replay verified | ✅ |
| Documentation updated | ✅ |
| Public interfaces documented | ✅ (JSDoc) |
| No TODO/FIXME | ✅ |
| No breaking contract changes | ✅ |
| Strict TS passes | ✅ |
| Lint passes | ✅ (strict compiler; per ID-003) |
| CI passes | ✅ (tsc + tests) |
| Review report published | ✅ this report |
| Explicit maintainer approval | ⏳ pending |

## 6. Verification evidence

- `npx tsc --noEmit` → **clean (exit 0)**.
- `npx tsx --test src/**/*.test.ts tests/regression/*.test.ts` → **35/35 PASS** (22 unit + 9 framework regression + 4 banking acceptance).
- **Golden dataset regression** printed above: 5/5 exact matches.

## 7. Frozen-layer integrity

- Runtime, framework, contracts, schemas, transport, platform services: **NOT modified**.
- Only additive changes: `Container.register()`, `Dependencies.runtimeCoordinator` (to allow WP-3 to consume RuntimeCoordinator per API baseline), tsconfig `resolveJsonModule`. No behavior of existing layers changed.
- Frozen assets copied read-only into `frozen-assets/`; the standards repo is untouched.

## 8. Rollback

Fully additive under `iips-platform/`. Delete the banking engine directory to roll back. No specification repo modified.

## 9. Program status

| Milestone | Status |
|---|---|
| IES-005 / 005.1 / 006 freeze | ✅ Approved / Frozen |
| WP-1 — Core Runtime | ✅ Approved |
| WP-2 — Framework Services | ✅ Approved |
| **WP-3 — Banking Engine** | **▶ COMPLETE — tsc clean, 35/35 tests, 4/4 acceptance criteria, awaiting approval** |
| WP-4 — Validation / Replay / Regression / Release | Pending |

**STOP — awaiting approval of WP-3 before WP-4 (Validation: golden dataset regression report, replay report, fixture acceptance, release candidate).**
